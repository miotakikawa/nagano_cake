<div class="container">
<div class="alert alert-dark col-5 d-flex" role="alert">
    <strong>商品新規登録</strong> 
</div>
</div>

<div class="row">
 <div class="col-md-6 offset-md-3">
<%= form_with model: @item, url: admin_genres_path, local:true do |f| %>
<table width="50%" class="table table-borderless">
 <tbody>
  <tr>
   <td>商品画像</td>
   <td><%= f.attachment_field :image %></td>
  </tr>
  <tr>
   <td>商品名</td>
   <td><%= f.text_field :item_name, class: "form-control" %></td>
  </tr>
  <tr>
   <td>商品説明</td>
   <td><%= f.text_field :explanation %></td>
  </tr>
  <tr>
   <td>ジャンル</td>
   <td> <%= f.collection_select :genre_id, Genre.all, :id, :name, :include_blank => "カテゴリを選択してください" %></td>
  </tr>
  <tr>
   <td>税抜価格</td>
   <td><%= f.text_field :cost %>円</td>
  </tr>
  <tr>
   <td>販売ステータス</td>
   <td><%= f.radio_button :status, :sale %>
  <%= f.label :status, "販売中", {value: :sale, style: "display: inline-block;"} %>
  <%= f.radio_button :status, :not_sale %>
  <%= f.label :status, "販売停止中", {value: :not_sale, style: "display: inline-block;"} %></td>
  </tr>
 </tbody>
</table>
 
 <div class="text-center">
 <span><%= f.submit '新規登録', class:"btn btn-success" %></span>
 </div>
<% end %>
</div>
</div>